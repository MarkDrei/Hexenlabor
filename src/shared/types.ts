// Placeholder for shared types
// Will contain game types, defense values, and utility types

export interface Position {
  x: number;
  y: number;
}

export interface Size {
  width: number;
  height: number;
}
